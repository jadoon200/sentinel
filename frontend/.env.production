# Production build: empty API URL => the dashboard calls the API on its own
# origin (relative paths). The single-service cloud image serves this built
# dashboard from the same FastAPI host, so no cross-origin URL or CORS is needed.
# For a split frontend/backend deploy instead, set this to the API's full origin
# (e.g. https://api.example.com).
VITE_API_URL=
