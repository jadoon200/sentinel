# Slim runtime for the read-only API image (Dockerfile.api / Dockerfile.deploy)
# — no ML stack. httpx and structlog are imported at API load (the ARGUS bridge
# and structured logging), so they belong here even though the ML deps don't.
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
structlog>=24.1
